# AZ.GPS

Independent iOS host-app GPS simulation library.

Development target: arm64, iOS 12 and later. Compatibility requires successful builds and device validation; universal iOS support is not claimed.

The original WolFoxa and dylib-analyzer projects remain untouched. Stage 1 source and GitHub Actions build configuration will be prepared on a separate review branch.
